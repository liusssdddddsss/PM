#设置端口号
server:
  port: 9090
  servlet:
    encoding:
      charset: UTF-8
      force: true
#连接数据库
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pm?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: emo1231
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
  mvc:
    encoding:
      charset: UTF-8
      force: true